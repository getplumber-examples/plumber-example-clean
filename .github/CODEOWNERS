# Code owners — required reviewers for branch protection (codeOwnerApprovalRequired).
# Configure "Require review from Code Owners" on the protected branch so Plumber's
# branchMustBeProtected control is satisfied when run with an API token.
*           @Totara-thib
/.github/   @Totara-thib
